# This GitHub repository does not provide Git LFS.
# Keep Unreal binary assets in ordinary Git and enforce the 100 MiB GitHub
# per-file limit through review and pre-push checks.
*.uasset -text
*.umap -text
*.ubulk -text
*.pak -text
