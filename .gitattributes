# The GitHub repository does not provide Git LFS, so the small vertical-slice
# assets in this repository are stored as ordinary Git binary files.
*.umap filter=lfs diff=lfs merge=lfs -text
*.ubulk filter=lfs diff=lfs merge=lfs -text
*.pak filter=lfs diff=lfs merge=lfs -text
